#ifndef __ROUND_H__
#define __ROUND_H__

#define round_i(x) ((int)round(x))

/*
inline int round_i(float v)
{
//	return (int)(v >= 0 ? v + 0.5f : v - 0.5f);
}

inline int round_i(const double& v)
{
//	return (int)(v >= 0 ? v + 0.5 : v - 0.5);
}
*/
#endif